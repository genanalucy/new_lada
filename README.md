# 115 Web 管理器

基于 `115cli` 的 115 网盘 Web 管理页面，可以查看目录、搜索、下载、上传、重命名和删除文件。

## 运行

```bash
python main.py
```

默认监听 `7200` 端口。

## 登录

打开页面后，在左侧粘贴 115 登录 Cookie：`UID`、`CID`、`SEID`、`KID`。

## 说明

- 下载由后端转发，浏览器直接保存。
- 上传支持多文件和文件夹。
- 登录信息会保存在本地 `data/auth.json`，可在页面里清除。
